#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	// Personal exercise below
	printf("This is still a personal exercise \n");
	printf("Let's see if this prints out too");
	// One thing to note: 
	// printf prints multiple lines together. 
	// You must explicitly add the new line (\n) on each line of code
	return (0);
}
